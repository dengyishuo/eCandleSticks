% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addCSPThreeLineStrike.R
\name{addCSPThreeLineStrike}
\alias{addCSPThreeLineStrike}
\title{Add CSP Three Line Strike Patterns to Candlestick Chart}
\usage{
addCSPThreeLineStrike(
  eCandleSticks_result,
  csp_three_line_result,
  bull_color = "blue",
  bear_color = "purple",
  point_size = 4,
  bull_shape = 24,
  bear_shape = 25,
  point_alpha = 0.8,
  mark_at_close = TRUE
)
}
\arguments{
\item{eCandleSticks_result}{The result object returned by eCandleSticks function}

\item{csp_three_line_result}{The result object from CSPThreeLineStrike analysis, which should be an xts object
containing logical columns 'Bull.ThreeLineStrike' and 'Bear.ThreeLineStrike'}

\item{bull_color}{Color for Bullish Three Line Strike points. Default "blue".}

\item{bear_color}{Color for Bearish Three Line Strike points. Default "purple".}

\item{point_size}{Size for the pattern points. Default 4.}

\item{bull_shape}{Shape for Bullish pattern points. Default 24 (up triangle).}

\item{bear_shape}{Shape for Bearish pattern points. Default 25 (down triangle).}

\item{point_alpha}{Alpha transparency for the pattern points. Default 0.8.}

\item{mark_at_close}{Whether to mark at the close price. If FALSE, marks at the high/low. Default TRUE.}
}
\value{
A modified eCandleSticks result list with Three Line Strike patterns added to the price plot
and the combined plot updated accordingly.
}
\description{
This function adds CSP Three Line Strike patterns (Bullish and Bearish)
to a candlestick chart created by eCandleSticks using the results from CSPThreeLineStrike analysis.
}
\examples{
\dontrun{
library(quantmod)
getSymbols("AAPL", src = "yahoo", from = "2023-01-01", to = "2025-09-08")

# Create candlestick chart
result <- eCandleSticks(AAPL)

# Get CSP Three Line Strike results
csp_three_line_data <- CSPThreeLineStrike(AAPL)

# Add Three Line Strike patterns
result_with_patterns <- addCSPThreeLineStrike(result, csp_three_line_data)

# Display the combined plot with pattern points
print(result_with_patterns$combined_plot)
}
}
