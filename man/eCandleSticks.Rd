% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eCandleSticks.R
\name{eCandleSticks}
\alias{eCandleSticks}
\title{Enhanced Candlestick Chart Plotting Function (based on ggplot2)}
\usage{
eCandleSticks(
  data,
  start_date = NULL,
  end_date = NULL,
  color_up = "#FF4444",
  color_down = "#44AA99",
  color_shadow = "#666666",
  show_volume = TRUE,
  color_volume_up = "#FF444480",
  color_volume_down = "#44AA9980",
  show_ma = FALSE,
  ma_periods = c(20, 50),
  color_ma = c("#FF8800", "#9900FF"),
  title = NULL,
  subtitle = NULL,
  theme_type = c("minimal", "classic", "dark"),
  axis_text_angle = 45,
  return_components = TRUE
)
}
\arguments{
\item{data}{Stock data object obtained by quantmod::getSymbols(), must be in xts/zoo format with OHLC columns
(Open, High, Low, Close), and can include Volume and Adjusted columns (OHLCVA).}

\item{start_date}{Start date (character/Date), e.g., "2023-01-01". Default NULL (uses earliest data date).}

\item{end_date}{End date (character/Date), e.g., "2023-12-31". Default NULL (uses latest data date).}

\item{color_up}{Color for rising candles (close >= open). Default "#FF4444" (red).}

\item{color_down}{Color for falling candles (close < open). Default "#44AA99" (green).}

\item{color_shadow}{Color for shadows (high-low). Default "#666666" (dark gray). Supports transparency (e.g., "#66666680").}

\item{show_volume}{Whether to plot volume subplot. Default TRUE; automatically disabled if no Volume column.}

\item{color_volume_up}{Volume color for rising days. Default "#FF444480" (semi-transparent red).}

\item{color_volume_down}{Volume color for falling days. Default "#44AA9980" (semi-transparent green).}

\item{show_ma}{Whether to plot moving averages (MA). Default FALSE.}

\item{ma_periods}{Vector of MA periods. Default c(20, 50) (20-day and 50-day MA).}

\item{color_ma}{Vector of MA colors, must match length of ma_periods. Default c("#FF8800", "#9900FF").}

\item{title}{Main chart title. Default auto-generated (e.g., "AAPL Stock Price (2023-01-01 to 2023-12-31)").}

\item{subtitle}{Chart subtitle. Default NULL.}

\item{theme_type}{Chart theme. Options: "minimal", "classic", "dark" (dark mode). Default "minimal".}

\item{axis_text_angle}{Rotation angle for x-axis date labels. Default 45 (degrees); 0 for horizontal.}

\item{return_components}{Whether to return the individual components as a list. Default TRUE.}
}
\value{
A list containing:
  - price_plot: The candlestick chart (ggplot object)
  - volume_plot: The volume subplot (ggplot object, or NULL if show_volume=FALSE)
  - data: The processed data used for plotting
  - combined_plot: The combined plot (if show_volume=TRUE, a cowplot combined object)
}
\description{
Plots highly customizable candlestick charts for stock data downloaded by quantmod (containing OHLCVA columns),
with support for custom colors, date ranges, volume subplots, and technical indicators (e.g., MA moving averages).
}
\examples{
\dontrun{
library(dplyr)
library(ggplot2)
library(quantmod)
getSymbols("AAPL", src = "yahoo", from = "2023-01-01", to = "2025-09-08") # Contains OHLCVA columns

# Basic usage (returns list with components)
result <- eCandleSticks(AAPL)
print(result$combined_plot)

# Access individual components
print(result$price_plot)
print(result$volume_plot)
head(result$data)
}
}
