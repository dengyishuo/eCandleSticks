% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addCSPThreeInside.R
\name{addCSPThreeInside}
\alias{addCSPThreeInside}
\title{Add CSP Three Inside Points to Candlestick Chart}
\usage{
addCSPThreeInside(
  eCandleSticks_result,
  csp_three_inside_result,
  three_inside_up_color = "green",
  three_inside_down_color = "red",
  point_size = 3,
  three_inside_up_shape = 24,
  three_inside_down_shape = 25,
  point_alpha = 0.8,
  mark_at_close = TRUE
)
}
\arguments{
\item{eCandleSticks_result}{The result object returned by eCandleSticks function}

\item{csp_three_inside_result}{The result object from CSP Three Inside analysis,
which should be a data frame containing a 'Date' column and logical columns
'ThreeInsideUp' and 'ThreeInsideDown'}

\item{three_inside_up_color}{Color for Three Inside Up points. Default "green".}

\item{three_inside_down_color}{Color for Three Inside Down points. Default "red".}

\item{point_size}{Size for the Three Inside points. Default 3.}

\item{three_inside_up_shape}{Shape for Three Inside Up points. Default 24 (up triangle).}

\item{three_inside_down_shape}{Shape for Three Inside Down points. Default 25 (down triangle).}

\item{point_alpha}{Alpha transparency for the Three Inside points. Default 0.8.}

\item{mark_at_close}{Whether to mark at the close price. Default TRUE.}
}
\value{
A modified eCandleSticks result list with Three Inside points added to the price plot
and the combined plot updated accordingly.
}
\description{
This function adds CSP Three Inside points (Three Inside Up and Three Inside Down)
to a candlestick chart created by eCandleSticks using the results from a CSP Three Inside analysis,
and recombines it with the volume subplot if it exists.
}
\examples{
\dontrun{
library(quantmod)
getSymbols("AAPL", src = "yahoo", from = "2023-01-01", to = "2025-09-08")

# Create candlestick chart
result <- eCandleSticks(AAPL)

# Get CSP Three Inside results
csp_three_inside_data <- CSPThreeInside(AAPL) # This returns a data frame with ThreeInsideUp and ThreeInsideDown columns

# Add Three Inside points
result_with_three_inside <- addCSPThreeInside(result, csp_three_inside_data)

# Display the combined plot with Three Inside points
print(result_with_three_inside$combined_plot)
}
}
